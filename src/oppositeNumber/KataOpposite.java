package oppositeNumber;

public class KataOpposite {
    public static int opposite(int number){
        return -number;

        // return number * -1;
        // return Math.negateExact(number);
    }
}
